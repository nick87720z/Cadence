# Cadence Session Startup
# Part 2 - Start JACK (or not), according to user settings

INSTALL_PREFIX="X-PREFIX-X"

STARTUP="$INSTALL_PREFIX/bin/cadence-session-start --session $STARTUP"

unset INSTALL_PREFIX
